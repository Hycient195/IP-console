import "./Navbar.css";
import { footerItems } from '../../constants';
import { useRef } from 'react';



const Navbar = () => {
  
  return(
    <header>
      <nav>

      </nav>
    </header>
  )
  
}

export default Navbar;